## Pulse backend environment variables
#
# Copy this file to ".env" and fill in real values.
#

# PostgreSQL connection string (example format)
# PG_CONN="postgresql://USER:PASSWORD@HOST:5432/DBNAME"
PG_CONN=""

# Comma-separated list of allowed origins for CORS.
# - If empty, the API will allow "*" but WITHOUT credentials.
# - If set, use explicit origins like "https://anoni.net,https://staging.anoni.net"
CORS_ALLOW_ORIGINS=""

# Only meaningful when CORS_ALLOW_ORIGINS is an explicit list (not "*").
# true/false, 1/0, yes/no are accepted.
CORS_ALLOW_CREDENTIALS="false"
