---
title: 截圖遮蔽
description: 在截圖或照片上拉方框，把不該外流的名字、頭像與對話填成實心黑色，全程在本機處理。輸出重新編碼，原檔的 metadata 與檔名都不會帶過去，交給你之前會逐像素確認每一處都是純黑。
icon: material/selection-remove
offline_assets:
  # 臉部偵測用的程式與級聯資料是動態載入的，頁面裡沒有 script 標籤。離線副本
  # 仍然要包含它們，不然存下這一頁的人在斷網時按了自動找出人臉不會有反應。
  - utils/vendor/pico/pico.js
  - utils/vendor/pico/facefinder
---

# :material-selection-remove: 截圖遮蔽

<div id="redact-tool"></div>

<script src="../../js/redact.js"></script>

## 什麼時候用得上

好幾個場景頁都有同一個動作：把對話或威脅的截圖交給第三方。[社運行動者](../scenarios/activist.md)要保留威脅訊息當證據，[LGBTQ+ 與性少數](../scenarios/lgbtq.md)遇到肉搜要記下威脅內容，家暴倖存者要把對話交給社工或律師。截圖裡除了對方，通常還有第三人的名字、頭像、電話，以及你自己的帳號。

- **群組對話的截圖要交給律師或平台檢舉**：對方的訊息要留著，同一個畫面裡其他成員的名字與頭像不該一起送出去。
- **把騷擾訊息公開求助**：貼出來之前遮掉自己的電話號碼、對方朋友的名字，還有頂端狀態列上的電信商與時間。
- **現場照片要投稿**：背景裡的門牌、車牌、旁觀者的臉，拿掉 metadata 之後仍然在畫面裡，要另外遮。

## 怎麼用

1. 把圖拖進來、點一下選檔案，或直接貼上。
2. 先按「自動找出人臉」可以少拉幾個框，它會把找到的臉框起來。機器畫的框跟你自己拉的一樣，刪得掉也蓋得掉。
3. 在要遮的地方按住拖出方框，放開就填成黑色。拖幾個都可以，拖錯了按「復原上一個」。
4. 按「產生遮好的圖」。頁面會把輸出實際解開一次，確認每一處都是純黑，才給你下載。

輸出的檔名固定是 `redacted.png` 或 `redacted.jpg`。截圖的原始檔名常帶著 app 名稱與精確到秒的時間，那也是一種洩漏。

## 為什麼是實心填色

馬賽克是把一塊區域的像素平均成大格，模糊是把像素跟周圍混在一起。兩種都保留了原本內容的統計特徵，文字尤其容易還原。Depix 這類針對馬賽克文字的開源還原工具 2020 年就公開了，只要字型與大小猜對，就能把格子對回字元。半透明的螢光筆效果更不用說，調一下亮度與對比就直接看見。

實心填色不留任何原本的資訊。方框裡的每一個像素都被同一個顏色取代，輸出檔裡沒有原內容的任何殘留。頁面在交給你之前會把輸出實際解開一次，逐像素檢查每一個方框，檢查沒過就不給下載。

## 輸出重新編碼過

跟[檔案 metadata 清除器](strip-metadata.md)相反，這一頁一定會重新編碼。遮蔽本來就要改像素，沒有無損的可能。重新編碼的好處是原檔裡的 EXIF、XMP、縮圖與其他欄位一個都不會帶過去，輸出裡只有畫面本身。

輸出格式跟著輸入走：JPEG 進來就輸出 JPEG，其他格式一律輸出 PNG。PNG 是無損格式，截圖用它不會再損失畫質。JPEG 會用品質 0.92 重新壓一次，照片幾乎沒有差別，但每一次重新壓縮都會多損失一點，同一張圖不要來回處理。

超過一千六百萬像素的圖會先縮到上限再處理，手機上的 canvas 無法處理更大的畫布。縮小只影響輸出的解析度，遮蔽的位置不受影響。畫面的色彩描述檔不會保留，廣色域的照片顏色可能略有差異。

## 自動找出人臉只是提案

按下「自動找出人臉」才會去拿偵測用的程式與資料，約 240 KB。只想自己拉框的人不會被迫下載它。

它抓得到的是正面、直立、在畫面裡夠大的臉。側臉、低頭、被口罩或頭髮遮住、離鏡頭太遠的臉會漏掉，背景紋理複雜的時候也可能多框幾個不是臉的地方。多框的按一下刪掉，漏掉的自己補。

眼鏡是常見的一種。眼睛周圍是這類偵測器取得訊號最多的地方，鏡框改變了那一帶的明暗，分數就會掉。實測同一張臉疊上合成鏡框，細框大約掉一半還救得回來，粗框與墨鏡則是分數整個崩掉，怎麼調都抓不到。畫面上有人戴墨鏡就當作它一定漏掉，直接自己拉框。

更要緊的是，認得出一個人的線索本來就不只有臉。名牌、識別證、刺青、車牌、門牌、特徵明顯的衣服、背景裡的招牌，這些偵測不會碰。機器畫完之後那一輪自己看，才是真正決定要遮什麼的那一步。

用的是 [pico.js](https://github.com/nenadmarkus/picojs){target="_blank"}，MIT 授權，兩百行左右的 JavaScript，看得懂的人可以自己讀完。它不需要 WebGL，只要瀏覽器開著 JavaScript 就能執行。圖片跟這一頁其他部分一樣不會離開你的裝置。

## 不做的事

不做全自動遮蔽。哪些東西該遮取決於你的處境，模型無法替你判斷，而漏掉一張臉的代價由你承擔。「自動找出人臉」那顆按鈕只幫你把框畫出來，按不按「產生遮好的圖」仍然是你的決定，說明見上一節。

不做模糊與馬賽克，理由見上一節。

不處理影片。

## 離線可用

跟這一區其他工具一樣，程式存進裝置之後斷網也能用，那就是圖片沒有偷偷送出去最直接的證明。

要把這一頁帶著走，見[離線閱讀](../offline.md)。
